To run process_spin_files, the directory containing libSpinsTree.so
must be included in your LD_LIBRARY_PATH environment variable, e.g., 
in your .bashrc, add
export LD_LIBRARY_PATH = $LD_LIBRARY_PATH:$HOME/nedm-sim/sim/DressedSpin/ROOTMacros

